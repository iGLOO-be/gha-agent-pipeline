# Local config (not secret, but consumer-specific)

- Copy `consumers.example.json` → `consumers.json` for the [gha-agent-pipeline-release](../.cursor/skills/gha-agent-pipeline-release/SKILL.md) skill.
- `consumers.json` is **gitignored** and must not be committed.
